name: Terraform Pipeline

on:
  push:
    branches:
      - feature-git_actions

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        run: git clone  https://github.com/AdiDimi/TerraformTest.git
      #   uses: actions/checkout@v2
      - name: Print Hello World
        run: echo "Hello World. GitHub Actions!"
      - name: sign in as root
        run: sudo su - root
      - name: accept outgoing traffic
        run: iptables -I OUTPUT -j ACCEPT
      - name: accept incoming traffic
        run: iptables -I INPUT -j ACCEPT
      - name: Ping something
        run: ping -c 5 google.com
      - name: List files
        run: ls -la
      # - name: Setup Terraform
      #   uses: hashicorp/setup-terraform@v1
      #   with:
      #     terraform_version: 0.12.29

      # - name: Terraform Init
      #   run: terraform init

      # - name: Terraform Plan
      #   run: terraform plan

      # - name: Terraform Apply
      #   run: terraform apply -auto-approve
